name: todo_list_app
description: A new Flutter project.

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"


dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_slidable: ^0.6.0
  table_calendar: ^3.0.1
  percent_indicator: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  

flutter:
  uses-material-design: true

  assets:
    - assets/images/splash/
    - assets/images/walkthrough/
    - assets/images/successful/

  
  

    

  fonts:
    - family: AvenirNextRoundedPro
      fonts:
        - asset: assets/fonts/AvenirNextRoundedPro.ttf
